# iOS App

This directory contains the SwiftUI source tree plus an `XcodeGen` project spec in `project.yml`.

## Generate The Project

Install Xcode and XcodeGen, then run:

```bash
xcodegen generate
```

The app defaults to `http://127.0.0.1:3000` for the backend so it works in the iOS simulator against the local API.

