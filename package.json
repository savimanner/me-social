{
  "name": "me-social",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/worker",
    "packages/contracts",
    "packages/core"
  ],
  "scripts": {
    "build": "npm run build --workspace @me-social/contracts && npm run build --workspace @me-social/core && npm run build --workspace @me-social/api && npm run build --workspace @me-social/worker",
    "test": "npm run test --workspace @me-social/contracts && npm run test --workspace @me-social/core && npm run test --workspace @me-social/api && npm run test --workspace @me-social/worker",
    "dev:api": "npm run dev --workspace @me-social/api",
    "dev:worker": "npm run dev --workspace @me-social/worker"
  }
}
